services:
  frontend-staging:
    image: ghcr.io/chandu-sasidharan/bbb-frontend:stage
    container_name: bbb-frontend-staging
    expose:
      - '3000'
    networks:
      - edge_net
    restart: unless-stopped

  backend-staging:
    image: ghcr.io/chandu-sasidharan/bbb-backend:stage
    container_name: bbb-backend-staging
    expose:
      - '4000'
    env_file:
      - ./backend/.env.stage
    networks:
      - edge_net
    restart: unless-stopped

networks:
  edge_net:
    external: true
